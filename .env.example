# Shell-compatible dotenv template for global agent/MCP credentials.
# Copy to .env, fill in the values, then run: make setup-aliases
# .env is git-ignored. Never commit real credentials.

# ── MCP Servers ───────────────────────────────────────────────────────────────
export CWB_MCP_TOKEN=""

# ── Other tool credentials ────────────────────────────────────────────────────
export EXA_API_KEY=""
export CONTEXT7_API_KEY=""
export FIRECRAWL_API_KEY=""
export STITCH_API_KEY=""
